# Repository Working Rules

- Commit and push each completed change set to `origin/main`.
- Do not commit `.env`, local databases, logs, caches, build output, or generated local artifacts.
- Run the relevant backend/frontend checks before claiming a change is complete.
- Keep provider credentials and local machine paths in `.env` or `.env.example` placeholders only.
